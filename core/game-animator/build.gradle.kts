plugins {
    `mikbot-plugin`
    `mikbot-module`
    `mikbot-publishing`
}

group = "dev.schlaubi.mikbot"
version = "2.5.1"

mikbotPlugin {
    description.set("Plugin changing the bots presence every 30 seconds")
}
